apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: mern

data:
  PORT: "3000"
  CLIENT_URL: "http://frontend-service"
  BASE_API_URL: "api"
  AWS_REGION: "us-east-1"
  AWS_BUCKET_NAME: ""